name: Frontend CI/CD (Docker)

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'app/**'
      - 'components/**'
      - 'api/**'
      - 'contexts/**'
      - 'lib/**'
      - 'package.json'
      - 'package-lock.json'
      - 'next.config.mjs'
      - 'tailwind.config.ts'
      - 'tsconfig.json'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'app/**'
      - 'components/**'
      - 'api/**'
      - 'contexts/**'
      - 'lib/**'
      - 'package.json'
      - 'package-lock.json'
      - 'next.config.mjs'
      - 'tailwind.config.ts'
      - 'tsconfig.json'
  workflow_dispatch:

env:
  NODE_VERSION: '18'
  NEXT_PUBLIC_SPRING_API_URL: ${{ secrets.NEXT_PUBLIC_SPRING_API_URL }}
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}

jobs:
  # 1. 테스트 및 빌드
  test-and-build:
    name: 🔧 Test & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Build application
        run: npm run build
        env:
          NEXT_PUBLIC_SPRING_API_URL: ${{ env.NEXT_PUBLIC_SPRING_API_URL || 'http://localhost:8080' }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-files
          path: .next/
          retention-days: 1

  # 2. Docker 이미지 빌드 및 푸시
  docker-build-and-push:
    name: 🐳 Build & Push Docker Image
    needs: test-and-build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKERHUB_USERNAME }}/seoul-bike-frontend
          tags: |
            type=ref,event=branch
            type=sha,prefix=main-
            type=raw,value=latest,enable=true

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_SPRING_API_URL=${{ env.NEXT_PUBLIC_SPRING_API_URL }}

  # 3. EC2 서버에 배포
  deploy:
    name: 🚀 Deploy to EC2
    needs: docker-build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    continue-on-error: true

    steps:
      - name: Check if server secrets are configured
        id: check-secrets
        run: |
          if [ -n "${{ secrets.SERVER_HOST }}" ] && [ -n "${{ secrets.SERVER_USERNAME }}" ] && [ -n "${{ secrets.SERVER_SSH_KEY }}" ]; then
            echo "server_configured=true" >> $GITHUB_OUTPUT
            echo "✅ Server secrets configured"
          else
            echo "server_configured=false" >> $GITHUB_OUTPUT
            echo "⚠️ Server deployment secrets not configured. Skipping deployment."
            echo "ℹ️ Docker image built and pushed successfully: ${{ env.DOCKERHUB_USERNAME }}/seoul-bike-frontend:latest"
          fi

      - name: Deploy via SSH
        if: steps.check-secrets.outputs.server_configured == 'true'
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            docker stop seoul-bike-frontend || true
            docker rm seoul-bike-frontend || true
            docker pull ${{ env.DOCKERHUB_USERNAME }}/seoul-bike-frontend:latest
            docker run -d \
              --name seoul-bike-frontend \
              --restart unless-stopped \
              -p 3000:3000 \
              -e NEXT_PUBLIC_SPRING_API_URL=${{ secrets.NEXT_PUBLIC_SPRING_API_URL }} \
              ${{ env.DOCKERHUB_USERNAME }}/seoul-bike-frontend:latest
            docker image prune -f

      - name: Skip deployment
        if: steps.check-secrets.outputs.server_configured == 'false'
        run: |
          echo "ℹ️ Server deployment skipped - secrets not configured"
          echo "✅ Docker image built and pushed successfully"
          echo "🔗 Image: ${{ env.DOCKERHUB_USERNAME }}/seoul-bike-frontend:latest"
          echo "💡 To enable server deployment, configure SERVER_HOST, SERVER_USERNAME, and SERVER_SSH_KEY secrets"

  # 4. 보안 스캔
  security-scan:
    name: 🛡️ Security Scan
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
          ignore-unfixed: true

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

  # 5. 완료 메시지
  notify:
    name: 📢 Notify Result
    needs: [test-and-build, docker-build-and-push]
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Notify success
        if: success()
        run: |
          echo "✅ Frontend CI/CD completed successfully!"
          echo "🚀 Deployed to EC2 via Docker"

      - name: Notify failure
        if: failure()
        run: |
          echo "❌ Frontend CI/CD failed!"
          echo "🔍 Check the logs for more details"
